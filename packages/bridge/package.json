{
  "name": "@cursor-notebook-bridge/bridge",
  "version": "0.1.0",
  "description": "Local bridge service — session lifecycle, backend routing, and output streaming",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@cursor-notebook-bridge/core": "workspace:*",
    "@cursor-notebook-bridge/backend-colab": "workspace:*",
    "express": "^4.18.0",
    "ws": "^8.17.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/ws": "^8.5.0",
    "@types/uuid": "^9.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
